import deval from "deval";

export default {
  name: "N Queens",
  code: deval(function() {
    /**
     * Let's get this party started!
     **/
    function foo() {
      const val = "bar";
      console.log(val);
      return "bar";
    }

    const answer = foo();
    return answer;
  })
};
